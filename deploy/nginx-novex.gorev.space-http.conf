# Временный HTTP-only конфиг до выдачи TLS-сертификата.
server {
    listen 80;
    listen [::]:80;
    server_name novex.gorev.space;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }
    location / {
        return 404;  # до certbot — ничего не светим
    }
}
